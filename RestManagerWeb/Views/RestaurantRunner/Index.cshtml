@model IEnumerable<RestaurantListItemVm>
@{
    ViewData["Title"] = "Restaurants";
}

<h2>Restaurants</h2>
<p>Create @Html.ActionLink("New", "New", "RestaurantRunner") restaurant configuration</p>

<div>
    <table class="table">
        <thead>
        <th>Configuration name</th>
        <th>Tables count</th>
        <th>&nbsp;</th>
        </thead>
        <tbody>
        @foreach(var configuration in Model)
        {
            <tr>
                <td>@Html.DisplayFor((m) => configuration.Name)</td>
                <td>@Html.DisplayFor((m) => configuration.TablesCount)</td>
                <td>@Html.ActionLink("Edit", "Details", "RestaurantRunner", new { id = configuration.Guid })</td>
            </tr>
        }
        </tbody>
    </table>
</div>
